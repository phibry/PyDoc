# Tuples: Immutable Lists
t = ('a', 'a', 'b')
t.count('a')  # How many a's
t.index('a')  # Index of first a in Tuple
# Usefull when saving things in smth and don't want to reasign it accidently.
