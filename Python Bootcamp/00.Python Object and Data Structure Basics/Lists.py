# Lists
newlist = ['one', 'two', 'three']
newlist.append('four')  # Add smth. to a list
# Save the Result of pop and remove it from the list (Default (-1))
popped_item = newlist.pop()

new_list = ['a', 'e', 'x', 'b', 'c']
new_list.sort()  # Returns a sorted list (alphabetical), Don't have to be assigned
new_list.reverse()  # Reverse the list

num_list = [4, 1, 8, 3]
num_list.sort()  # Returns the sorted list
num_list.reverse()  # Reverse the list
